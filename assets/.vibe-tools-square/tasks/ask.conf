# Task Definition: ask
TASK_NAME="ask"
TASK_DESCRIPTION="Direct query with simple prompt-based questions"
TASK_TYPE="ask"

# Model and Provider 
TASK_PROVIDER="gemini"
TASK_MODEL="free"

# Prompt
PROMPT="How can I help you today?"

# Parameters (will be appended to the command)
PARAM_MAX_TOKENS="4000"

# Usage: ./run-prompt.sh ask --prompt="How many cats are there in the world?" [--max-tokens="8000"] [--provider=gemini] [--model=gemini-2.5-flash] [--go]
