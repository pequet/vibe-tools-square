# Task Definition: hello
TASK_NAME="hello"
TASK_DESCRIPTION="A simple hello world task to demonstrate task configuration and template functionality"
TASK_TYPE="ask"

# Template Configuration
TASK_TEMPLATE="hello"

# Model and Provider Configuration
TASK_PROVIDER="gemini"
TASK_MODEL="free"

# Default Parameters - All parameters are provided here so user only needs to run "./run-prompt.sh hello"
# Parameters with placeholders can be overridden: "./run-prompt.sh hello --user_name=Bobby"
# NOTE: --go is NOT included here so user can choose dry-run vs execution mode
TASK_DEFAULT_PARAMS="--max-tokens=4000 --name=\"{{USER=Test User}}\" --project=\"{{PROJECT=Hello World Project}}\" --description=\"Testing the task configuration system\" --documentation=file:README.md --output-file=\"{{REPO_ROOT}}Core/Models/0. Inbox/\$(date +%Y-%m-%d_%H%M)-hello-world.md\""
