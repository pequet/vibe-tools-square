# Task Definition: hello
TASK_NAME="hello"
TASK_DESCRIPTION="A simple hello world task to demonstrate task configuration and template functionality"
TASK_TYPE="ask"

# Template Configuration
TASK_TEMPLATE="hello"

# Model and Provider Configuration
TASK_PROVIDER="gemini"
TASK_MODEL="free"

# Default Parameters 
TASK_DEFAULT_PARAMS="--max-tokens=4000 --name=\"{{USER=Test User}}\" --project=\"{{PROJECT=Hello World Project}}\" --description=\"Testing the task configuration system\" --documentation=\"file:{{REPO_ROOT}}README.md\" --output-file=\"{{REPO_ROOT}}Core/Models/0. Inbox/\$(date +%Y-%m-%d_%H%M)-hello-world.md\""

# Usage: ./run-prompt.sh hello --user="Bobby" --project="Kill Laura Palmer" --repo-root="path/to/repo/" [--go]
