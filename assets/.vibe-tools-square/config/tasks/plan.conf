# Task Definition: plan
TASK_NAME="plan"
TASK_DESCRIPTION="Generate detailed implementation plans based on codebase analysis"
TASK_TYPE="plan"

# Dual Model Configuration (Plan uses both file and thinking models)
TASK_FILE_PROVIDER="gemini" 
TASK_FILE_MODEL="free"
TASK_THINKING_PROVIDER="gemini"
TASK_THINKING_MODEL="free"

# Prompt
PROMPT="Create an implementation plan for this project"

# Parameters (will be appended to the command)
PARAM_INCLUDE="README.md,src/"
PARAM_EXCLUDE=""

# Usage: ./run-prompt.sh plan --prompt="What is this project about?" --include="README.md,src" --exclude="src/utils" [--thinking-provider=gemini] [--thinking-model=gemini-2.5-flash] [--file-provider=gemini] [--file-model=gemini-2.5-flash] [--go]
