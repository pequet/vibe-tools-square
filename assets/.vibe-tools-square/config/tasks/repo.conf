# Task Definition: repo
TASK_NAME="repo"
TASK_DESCRIPTION="Analyze repository context"
TASK_TYPE="repo"

# Model and Provider Configuration
TASK_PROVIDER="gemini"
TASK_MODEL="free"

# Prompt
PROMPT="Explain what this repository is about"

# Parameters (will be appended to the command)
PARAM_INCLUDE="README.md,src/"
PARAM_EXCLUDE=""

# Usage: ./run-prompt.sh repo --prompt="What is this project about?" --include="README.md,src" --exclude="src/utils" [--provider=gemini] [--model=gemini-2.5-flash] [--go]
