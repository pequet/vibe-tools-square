# Vibe-Tools Square Default Configuration
# This file contains environment variables and default settings

# Prefix for global command installation (default: core)
# Results in: ${GLOBAL_COMMAND_PREFIX}-run-prompt.sh
GLOBAL_COMMAND_PREFIX="core"

